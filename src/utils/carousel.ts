const sliders: Element | null = document.querySelector('.carousel-box');
var scrollClick: number;
var scrollAmount: number = 0;

const scrollLeft = () => {
  
};

const scrollRight = () => {
  
};

export { scrollLeft, scrollRight };
